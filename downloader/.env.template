NEXTCLOUD_URL=https://divyanshu-palak-wedding.space
NEXTCLOUD_USERNAME=your_username
NEXTCLOUD_PASSWORD=your_password
MAX_CONCURRENT_DOWNLOADS=4

# Path to your drives/volumes on the HOST machine (outside Docker)
# Mac:     VOLUMES_HOST_PATH=/Volumes        (default, can omit)
# Windows: VOLUMES_HOST_PATH=C:/             (or D:/ etc — add to Docker Desktop file sharing first)
VOLUMES_HOST_PATH=/Volumes
